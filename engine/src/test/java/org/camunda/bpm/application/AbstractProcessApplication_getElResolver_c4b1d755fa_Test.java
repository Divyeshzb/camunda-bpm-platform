// Test generated by RoostGPT for test camunda-engine-unit using AI Type Open AI and AI Model gpt-4

package org.camunda.bpm.application;

import org.camunda.bpm.impl.juel.jakarta.el.ELResolver;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.mockito.Mockito;

import static org.junit.jupiter.api.Assertions.*;
import static org.mockito.Mockito.when;

public class AbstractProcessApplication_getElResolver_c4b1d755fa_Test {

    private AbstractProcessApplication abstractProcessApplication;
    private ELResolver elResolver;

    @BeforeEach
    public void setup() {
        abstractProcessApplication = Mockito.mock(AbstractProcessApplication.class);
        elResolver = Mockito.mock(ELResolver.class);
    }

    @Test
    public void testGetElResolver_WhenResolverIsNull() {
        when(abstractProcessApplication.getElResolver()).thenReturn(null);
        assertNull(abstractProcessApplication.getElResolver());
    }

    @Test
    public void testGetElResolver_WhenResolverIsNotNull() {
        when(abstractProcessApplication.getElResolver()).thenReturn(elResolver);
        assertNotNull(abstractProcessApplication.getElResolver());
        assertEquals(elResolver, abstractProcessApplication.getElResolver());
    }
}
